# 魚盛サイト 差し替え・更新メモ

`index.html` 1ファイル完結。テキストエディタで開いて該当箇所を書き換えれば反映される。

## 0. 画像ファイルの保存（最優先）

会話で送ってもらった画像は、Cowork のフォルダに**手動で保存**しないとサイトに表示されません。
`/Users/th/Documents/Claude/Projects/魚盛広告マーケティング/` に下記ファイル名で保存：

| ファイル名 | 使う画像 | 使用箇所 |
|---|---|---|
| `logo.png` | 円形ロゴ（藍×白） | ヘッダー左上、ファビコン、OG画像 |
| `hero.jpg` | 4代目本人＋暖簾＋花スタンドの開店日写真 | ヒーロー背景、継承セクション右 |
| `legacy.jpg` | 父（先代）の開店日写真 | 継承セクション左 |
| `interior.jpg` | 内装＋胡蝶蘭などの店内写真 | 店主の想いセクション内画像 |

**保存方法**：
1. このCoworkチャットの画像をクリック → 別ウィンドウで開く（または右クリック）
2. 「画像を保存」または保存先指定でデスクトップ等に保存
3. Finderで上記フォルダにドラッグ＆ドロップ
4. 上記の正確なファイル名にリネーム

ファイルが揃えば、`index.html` をブラウザでリロードするだけで自動反映されます。

## 1. 確定したらテキスト差し替え

| 項目 | 現状 | 該当箇所（HTML内） |
|---|---|---|
| 住所 | 神奈川県横浜市西区浅間町1-4-12 ✅反映済 | `<dt>所在地</dt>` 直下の `<dd>` |
| 営業時間（具体時間） | 「ランチ・ディナー」 | `<dt>営業時間</dt>` 直下の `<dd>` |
| 定休日 | 「不定休」 | `<dt>定休日</dt>` 直下の `<dd>` |
| 店主名 | 「斉藤 晃宏」 | `class="sig"` の `― 四代目 ...` |

## 2. 既存設定（自動で入っているもの）

- ロゴ画像 → ヘッダー左上 / favicon / OGP（`logo.png`）
- Instagram → `https://www.instagram.com/uosei_4th/`
- LINE公式 → `https://lin.ee/88l1q6c`
- 電話 → `045-311-2767`
- 決済 → 現金 / VISA・Master・American Express

## 3. 公開方法（候補）

- **すぐ公開**：[Netlify Drop](https://app.netlify.com/drop) にフォルダごとドラッグ → 即URL発行
- **独自ドメイン**：お名前.comなどでドメイン取得後、Netlify/Vercelに紐付け
- **既存サーバー**：FTPで `index.html` と画像群をアップ

## 4. 今後の追加候補

- Googleマップ埋め込み（住所確定後）
- メニュー写真（撮影後、`.menu-card` に追加）
- 食べログ導線（必要ならSNS欄に追加）
- ニュース/休業告知の差し替え運用（メニュー下に小さい告知欄を作る案）
